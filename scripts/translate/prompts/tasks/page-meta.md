Translate the following JSON containing SEO page titles and descriptions for the Denver For All civic platform. These appear in browser tabs and search engine results.

Return a valid JSON object with the EXACT SAME structure and keys, but with translated values. Do not translate JSON keys — only translate the string values.

Guidelines:
- Keep titles concise (under 60 characters where possible)
- Keep descriptions under 160 characters where possible (for SEO)
- "Denver For All" should remain untranslated in titles

Output ONLY the JSON — no markdown code fences, no explanation.
